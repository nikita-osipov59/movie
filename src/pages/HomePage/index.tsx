import { BorderPanel } from "@/components/ui";
import { MovieRandom } from "@/components";

export const HomePage = () => {
  return (
    <>
      <section>
        <MovieRandom />
      </section>
    </>
  );
};
